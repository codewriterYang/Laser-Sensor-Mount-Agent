services:
  backend:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./uploads:/app/uploads
      - ./exports:/app/exports
      - ./data/bom:/app/data/bom
      - ./logs:/app/logs
    restart: unless-stopped
